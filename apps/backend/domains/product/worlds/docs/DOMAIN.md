Worlds domain (admin): manage AI world templates and characters.

Storage: in-memory adapter (swappable).
Security: moderator+ required; CSRF enforced for mutations.

Tenancy: tenant_id comes from query param.

