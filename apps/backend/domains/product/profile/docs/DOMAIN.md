Profile domain: update username with IAM policy checks and outbox event.

Adapters: SQL repo, Redis outbox (DDD platform).

API и процессы настроек профиля описаны в
`../../../../../../docs/features/profile-settings/README.md`.
