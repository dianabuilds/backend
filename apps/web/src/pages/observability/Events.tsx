import React from 'react';

import { ObservabilityEvents } from '../../features/observability';

export default function ObservabilityEventsPage(): React.ReactElement {
  return <ObservabilityEvents />;
}
